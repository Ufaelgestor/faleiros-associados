{
  "name": "faleiros-associados",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "ci:build": "GITHUB_ACTIONS=1 vite build",
  "builder": "python3 tools/make_webp.py && npm run build"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
